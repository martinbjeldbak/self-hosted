*.raspberrypi.local {
  tls me@martinbjeldbak.com {
    dns cloudflare {env.CLOUDFLARE_API_TOKEN}
  }

  @dns host dns.raspberrypi.local
  handle @dns {
    encode gzip
    reverse_proxy adguardhome:3000
  }

  # osv..

  # Fallback for otherwise unhandled domains
  handle {
    abort
  }
}
